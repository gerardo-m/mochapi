<div class="mx-auto max-w-screen-lg w-full"
  <% if is_new_record?%>
    data-controller="text-focus"
  <% end%>
>
  <%= link_to "<", project_endpoints_path(project_id: @endpoint.project.id), class: "ml-2 rounded-lg py-3 pr-5 inline-block font-medium text-4xl text-blue-600 hover:text-blue-800" %>
  <h1 class="font-bold text-4xl inline">
    <%= is_new_record? ? "New" : "Editing"%> endpoint
  </h1>

  <%= render "form", endpoint: @endpoint, url: endpoint_path %>
</div>
