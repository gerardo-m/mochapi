<div class="mx-auto max-w-screen-lg w-full" data-controller="text-focus">
  <h1 class="font-bold text-4xl">New endpoint</h1>

  <%= render "form", endpoint: @endpoint, url: endpoint_url(id: @endpoint.id) %>

  <%= link_to "Cancel", project_endpoints_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
