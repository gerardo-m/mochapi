<% model_name ||= model.class.to_s.humanize %>
<%= button_to  model, method: :delete, class: "flex items-center", form: {onsubmit: "return confirm('Are you sure you want to delete this #{model_name}');"} do%>
  <%= render 'components/icons/delete_icon', size: size%>
<% end%>