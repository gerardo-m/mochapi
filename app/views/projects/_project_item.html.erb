<div class="flex flex-row px-5 border-2 border-blue-200 rounded-2xl">

  <div class="flex flex-row w-full h-10">
    <div class="flex items-center w-1/3 text-2xl font-bold"> <%= project.name%></div>
    <div class="flex items-center"><%= project.endpoints.count%> endpoints</div>
  </div>
  <div class="w-24 flex flex-row items-center justify-evenly">
    <%= render "components/show_icon_button", url: project_path(project), size: "2rem" %>
    <%= render "components/edit_icon_button", url: edit_project_path(project), size: "2rem" %>
  </div>
</div>