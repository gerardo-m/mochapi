<div class="mx-auto max-w-screen-lg w-full"
  <% if is_new_record? %>
    data-controller="text-focus"
  <% end %>
>
  <%= link_to "<", projects_path, class: "ml-2 rounded-lg py-3 pr-5 inline-block font-medium text-4xl lnk-primary" %>
  <h1 class="font-bold text-4xl inline">
    <%= is_new_record? ? "New" : "Editing"%> project
  </h1>

  <%= render "form", project: @project %>

  
</div>
